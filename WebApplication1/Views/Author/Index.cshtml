
@model List<WebApplication1.Models.Author>


@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@* First way  
@if (ViewBag.Authors.Count == 0)
{
    <h2>No Authors Found</h2>
}
else
{
    <h2>Here are the Authors</h2>
    <h3>@ViewBag.Author.Count</h3>
    <ul>
        @foreach (var author in ViewBag.Authors)
        {
            <li>@author.PenName</li>

        }
        <
    </ul>
}*@

@*Second way add the model at the top*@
@if (Model.Count == 0)
{
<h2>No Authors Found</h2>
}
else
{
<h2>Here are the Authors</h2>
<h3>@Model.Count</h3>
<ul>
    @foreach (var author in ViewBag.Authors)
    {
        <li>@author.PenName</li>

    }
    <
</ul>
}*@